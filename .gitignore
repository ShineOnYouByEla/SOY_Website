node_modules/
# Nur im Wurzelverzeichnis ignorieren: die statische Seite braucht kein npm.
# backend/package.json gehoert dagegen ins Repository.
/package-lock.json
/package.json
.DS_Store

# Lighthouse-CI-Reports (lokale Läufe)
.lighthouseci/

# Die gerenderten Katalogseiten entstehen beim Veröffentlichen aus den PDFs
# (scripts/build-katalog-seiten.mjs) und gehören nicht ins Repository: Es
# sind rund 55 MB, die sich bei jedem neuen Katalog erneut ansammeln würden.
# In GitHub Actions übernimmt das ein Cache, der an den PDF-Dateien hängt.
/kataloge/seiten/
